@model RT.Models.Kitchen

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)
      @Html.HiddenFor(model => model.KitchenID)
   <div class="CSSEditTableGenerator">
      <table>
            <tr>
                <td>
                       @Html.LabelFor(model => model.KitchenName)
                </td>
            </tr>
           <tr>
                <td>
                   @Html.EditorFor(model => model.KitchenName)
                   @Html.ValidationMessageFor(model => model.KitchenName)
                </td>
            </tr>
        </table>
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>

}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
